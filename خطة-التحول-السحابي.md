# خطة التحول إلى منتج SaaS سحابي (اشتراكات)

توثيق لقرارات النقاش الخاص بتحويل البرنامج من تطبيق ويندوز أوفلاين (Tauri) إلى
منتج ويب تجاري (PWA) بقاعدة بيانات مركزية على Cloudflare، يُباع بنظام اشتراكات
لعدة زبائن (محلات) بشكل مستقل عن بعض.

هذا المستند خطة معمارية فقط — لا يعكس أي تنفيذ فعلي بعد.

## القرارات المحسومة

- **يُلغى تماماً** تطبيق الويندوز الأوفلاين الحالي (Tauri) — لا يبقى منتجين
  بالتوازي. PWA واحد يصير المنتج الوحيد.
- **تعدد المستأجرين:** قاعدة بيانات Cloudflare D1 **منفصلة لكل محل (Tenant)** —
  عزل فيزيائي كامل بين بيانات الزبائن، بدل الاعتماد على عمود `tenant_id`
  بقاعدة مشتركة.
- **الحسابات:** كل مستخدم مرتبط بحساب وحالة اشتراك (فعّال / تجربة / منتهي)،
  يُتحقق منها بكل طلب على السيرفر.
- **الدفع:** يدوي بالكامل بالمرحلة الأولى — الزبون يدفع بطريقة محلية (تحويل
  بنكي، زين كاش، إلخ) خارج النظام، ثم يُفعَّل اشتراكه يدوياً من لوحة التحكم.
  لا حاجة لبوابة دفع دولية (Stripe غير متاح للتجار بالعراق أصلاً).
- **لوحة التحكم الإدارية:** تشمل إدارة الاشتراكات، وتشمل أيضاً "بناء زبون
  جديد" بشكل شبه آلي (إنشاء قاعدة بياناته، تشغيل المخطط عليها، إنشاء حسابه)
  دون تدخل يدوي بالكود أو قاعدة البيانات.
- **الإنترنت:** مستقر بشكل عام بالعراق (خصوصاً على خطوط الهاتف النقال)، مما
  يجعل الاعتماد الكامل على اتصال دائم بالسيرفر خياراً مقبولاً.

## البنية العامة

```
[متصفح الزبون - ويندوز/أندرويد/أي جهاز]
              |
              v
   Cloudflare Pages (واجهة React واحدة، PWA متجاوبة)
              |
              v
   Cloudflare Workers (API + التحقق من الجلسة والاشتراك)
        /                              \
       v                                v
Control-Plane D1                  D1 خاصة بكل محل
(سجل الزبائن، حالة الاشتراك،        (نفس مخطط SQLite الحالي،
 معرف قاعدة بيانات كل زبون)          معزولة عن باقي الزبائن)
```

## المراحل الخمس

### المرحلة 1 — الأساس (Control Plane + قالب قاعدة بيانات الزبون)

- تصميم وبناء قاعدة `Control Plane` (D1): سجل الزبائن، حالة الاشتراك، تاريخ
  الانتهاء، معرف قاعدة بيانات كل زبون.
- تصميم نظام حسابات وجلسات (تسجيل دخول) مرتبط بالزبون (Tenant).
- تحويل مخطط SQLite الحالي إلى "قالب" D1 قابل للتكرار لكل زبون جديد — مع
  اعتماد UUID كمفاتيح أساسية (ما عاد إلزامياً تقنياً بعد إلغاء المزامنة بين
  أجهزة متعددة، لكنه أفضل أمنياً لمنع تخمين تسلسل البيانات عبر الروابط).
- **المُخرج:** بيئة زبون واحد تجريبي، يعمل يدوياً، يثبت صحة الفكرة تقنياً.

### المرحلة 2 — نقل منطق العمل إلى API

- إعادة كتابة منطق العمل الحالي (خوارزمية توزيع الدفعات، تحويل العملة،
  قواعد التحقق، المعاملات المالية الذرية) من Rust/Tauri Commands إلى
  Cloudflare Workers (TypeScript).
- كل نقطة API تتحقق أولاً من الجلسة، وتحدد قاعدة بيانات الزبون الصحيحة، وتتحقق
  من صلاحية الاشتراك قبل تنفيذ أي عملية.
- **المُخرج:** واجهة برمجية (API) كاملة تكرر كل وظائف التطبيق الحالي، مُختبرة
  على الزبون التجريبي من المرحلة 1.

### المرحلة 3 — واجهة الويب (PWA)

- تعديل واجهة React الحالية: استبدال كل نداءات `invoke()` بنداءات `fetch()`
  على الـ API الجديد.
- إضافة شاشات تسجيل الدخول/إنشاء الحساب (غير موجودة بالنسخة الأوفلاين أصلاً).
- تعديلات تنسيقية لتكون الواجهة متجاوبة (Responsive) بين الجوال والحاسوب —
  نفس البيانات، تخطيط مختلف فقط.
- إضافة ملف PWA Manifest و Service Worker (تثبيت كأيقونة، تخزين مؤقت للأصول
  لسرعة التحميل — وليس لتخزين البيانات نفسها أوفلاين).
- نشر الواجهة على Cloudflare Pages.
- **المُخرج:** منتج متكامل يعمل فعلياً من متصفح الجوال والحاسوب لزبون تجريبي
  واحد.

### المرحلة 4 — لوحة التحكم الإدارية + التفعيل التلقائي للزبون + الفوترة اليدوية

- بناء لوحة تحكم منفصلة عن تطبيق الزبائن: قائمة الزبائن، حالة/تاريخ انتهاء كل
  اشتراك، وزر تفعيل/تمديد يدوي.
- بناء تدفق "زبون جديد" شبه آلي: نموذج إدخال بيانات المحل → إنشاء قاعدة D1
  جديدة تلقائياً → تشغيل المخطط عليها → إنشاء حساب المالك → تسجيله بسجل
  الزبائن بحالة أولية (تجربة أو بانتظار الدفع).
- ربط فحص انتهاء الاشتراك فعلياً بكل طلبات الـ API (حجب الوصول عند الانتهاء،
  مع رسالة واضحة للزبون تطلب التجديد).
- **المُخرج:** تقدر تضيف زبون تجاري جديد بالكامل من لوحة التحكم فقط، بدون أي
  تدخل يدوي بالكود أو القاعدة.

### المرحلة 5 — التحصين، الاختبار، والإطلاق التجاري

- مراجعة أمنية مركزة: التأكد إن عزل البيانات بين الزبائن غير قابل للاختراق،
  وأن فحص الاشتراك لا يمكن تجاوزه.
- اختبار التكلفة والحمل على حدود Cloudflare D1/Workers (المجانية مقابل
  المدفوعة) لتقدير تكلفة كل زبون إضافي، وبناء التسعير على أساسها.
- رسالة ختامية لمستخدمي النسخة الأوفلاين الحالية (إن وُجدوا) توجّههم للمنتج
  الجديد.
- إطلاق تجريبي محدود مع عدد صغير من الزبائن الحقيقيين عبر نموذج الدفع/التفعيل
  اليدوي، وجمع ملاحظاتهم قبل التوسع.
- **المُخرج:** منتج تجاري حي يستقبل زبائن حقيقيين يدفعون فعلياً.

## نقاط مفتوحة لمراحل لاحقة (خارج نطاق هذه الخطة)

- أتمتة الدفع (بوابة دفع محلية) بعد ثبوت الطلب وتجاوز مرحلة الدفع اليدوي.
- تذكيرات تلقائية (بريد/رسالة) قبل انتهاء الاشتراك.
- إمكانية عمل أوفلاين جزئي مستقبلاً (خارج نطاق هذه الخطة تماماً بعد قرار
  الإلغاء الحالي).
