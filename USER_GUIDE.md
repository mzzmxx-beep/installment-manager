# دليل إدارة الأقساط

**الإصدار:** 1.1.0
**المستودع:** https://github.com/mzzmxx-beep/installment-manager

نظام سطح مكتب (Windows) لإدارة المبيعات بالتقسيط: الزبائن، الكفلاء، المنتجات، جداول الأقساط، الدفعات،
التقارير، والنسخ الاحتياطي — يعمل محلياً بالكامل دون إنترنت، بقاعدة بيانات واحدة تحفظ كل شيء.

هذا الملف يجمع دليل الاستخدام الكامل وقائمة كل مزايا النظام في مكان واحد. يقابله نسختان بصريتان
منشورتان (روابط خاصة، قابلتان للطباعة):

- **دليل إدارة الأقساط** (دليل تفاعلي بقائمة جانبية): https://claude.ai/code/artifact/1bc3c595-53cd-492d-9bff-a63fe99368c3
- **إنفوجرافيك إدارة الأقساط** (ملخص بصري بصفحة واحدة): https://claude.ai/code/artifact/da3f2200-29d7-44ec-8221-96f26127a17c

---

## قائمة المزايا السريعة

| # | الميزة | الوصف |
|---|---|---|
| 1 | إدارة الزبائن | بحث فوري بالاسم أو الرقم الوطني، وسجل كامل لكل زبون |
| 2 | كفيل لكل فاتورة | كل عملية بيع يمكن أن تحمل كفيلاً مستقلاً من بين الزبائن أنفسهم |
| 3 | معرض المستمسكات | صور هوية وعقود قابلة للتصفح والحذف، محفوظة مع بيانات الزبون |
| 4 | بيع بالتقسيط | منتجات متعددة، هامش نسبة أو مبلغ مقطوع، وجدول أقساط يُولَّد تلقائياً |
| 5 | دفعات ذكية التوزيع | كل دفعة تُوزَّع على أقدم الأقساط أولاً، عبر كل الفواتير والعملات |
| 6 | متابعة المتأخرات | قائمة واحدة بكل الأقساط المتأخرة في المتجر، مرتبة حسب الأولوية |
| 7 | تقارير وتحليلات | الأرباح، الأكثر مبيعاً، أكثر الزبائن شراءً، والأكثر تأخراً — بلمحة |
| 8 | تصدير CSV | كل تقرير قابل للتنزيل فوراً كملف جاهز لإكسل، بدعم كامل للعربية |
| 9 | كشف حساب للطباعة | صفحة طباعة أنيقة لكل زبون، جاهزة للتصدير كـ PDF |
| 10 | نسخ احتياطي مرن | حفظ محلي أو في مجلد Google Drive / OneDrive بضغطة زر |
| 11 | تحديث تلقائي | تحقق من أحدث إصدار وتثبيته من داخل البرنامج مباشرة عبر GitHub |
| 12 | ترخيص محمي بالجهاز أو تجربة مجانية | تفعيل موقَّع رقمياً ومربوط ببصمة العتاد، أو تجربة مجانية ٣٠ يوماً بلا رمز، كلاهما محمي من تعديل ساعة النظام |
| 13 | تقرير تحويل العملة | يشرح كل عملية تحويل بين الدولار والدينار حدثت فعلاً، بالفاتورة والزبون والجهاز |
| 14 | دقة مالية كاملة | كل الأرقام أعداد صحيحة (لا كسور عشرية)، وسجل المبيعات/الدفعات غير قابل للتعديل أو الحذف |

---

## الفهرس

- [قبل البدء: تفعيل الترخيص](#قبل-البدء-تفعيل-الترخيص)
- [العملاء](#العملاء)
- [صفحة الزبون](#صفحة-الزبون)
- [مستمسكات الزبون](#مستمسكات-الزبون)
- [تسجيل الدفعات](#تسجيل-الدفعات)
- [المنتجات](#المنتجات)
- [بيع جديد بالتقسيط](#بيع-جديد-بالتقسيط)
- [المتأخرات](#المتأخرات)
- [التقارير والتحليلات](#التقارير-والتحليلات)
- [تقرير تحويل العملة](#تقرير-تحويل-العملة)
- [كشف حساب الزبون](#كشف-حساب-الزبون)
- [النسخ الاحتياطي](#النسخ-الاحتياطي)
- [تحديث البرنامج](#تحديث-البرنامج)
- [الأمان وحماية البيانات](#الأمان-وحماية-البيانات)

---

## قبل البدء: تفعيل الترخيص

البرنامج مرتبط برخصة استخدام واحدة لكل جهاز (تُحسب بصمة العتاد تلقائياً). عند أول تشغيل تظهر شاشة
"تفعيل إدارة الأقساط" وأمامك خياران:

1. **الصق رمز الترخيص** الذي استلمته من المورّد في الحقل ثم اضغط **تفعيل**.
2. أو اضغط **"ابدأ التجربة المجانية (٣٠ يوماً)"** لاستخدام البرنامج بكامل ميزاته لمدة شهر واحد دون أي
   رمز — متاح مرة واحدة فقط لكل جهاز.

| الحالة | ماذا تعني وماذا تفعل |
|---|---|
| رمز غير صالح | الرسالة تحدد السبب مباشرة تحت الحقل — تأكد من نسخ الرمز كاملاً بدون فراغات زائدة |
| ترخيص منتهي | يظهر تاريخ الانتهاء، ويمكنك لصق رمز ترخيص جديد بنفس الشاشة للمتابعة |
| انتهت الفترة التجريبية | بعد مرور ٣٠ يوماً على بدء التجربة يُقفل البرنامج، وتظهر شاشة فيها اسم الشركة ورقم الهاتف والبريد الإلكتروني للتواصل والحصول على ترخيص كامل — ثم يُلصق رمز الترخيص في نفس الشاشة للتفعيل |
| تم إيقاف التطبيق | حماية من تلاعب بساعة النظام: إن رجعت ساعة الجهاز إلى وقت أقدم من آخر تشغيل مسجَّل، يُقفل البرنامج بالكامل. الحل: أعد ضبط ساعة النظام للوقت الصحيح الحالي ثم أعد التشغيل |

> **ملاحظة:** الترخيص (والتجربة المجانية) مربوط بجهاز واحد. عند نقل البرنامج إلى حاسوب آخر (وليس مجرد
> استعادة نسخة احتياطية عليه) يلزم رمز ترخيص جديد لبصمة العتاد الجديدة — تواصل مع المورّد للحصول عليه.
> تفاصيل الترخيص الحالي (تاريخ التفعيل، تاريخ النفاذ، مدة الترخيص) تظهر أيضاً في تبويب "حول البرنامج".

---

## العملاء

التبويب الأول — قائمة كل الزبائن، والبحث، وإضافة زبون جديد. القائمة قابلة للبحث بالاسم أو الرقم
الوطني. اضغط على أي زبون في القائمة لفتح صفحته الكاملة.

**نموذج "إضافة عميل جديد":**

| الحقل | الوصف |
|---|---|
| الاسم * | إلزامي |
| الرقم الوطني * | إلزامي — يُستخدم أيضاً في مربع البحث |
| الهاتف | اختياري |
| العنوان | اختياري |

---

## صفحة الزبون

مركز كل شيء يخص عميلاً واحداً: أرصدته، فواتيره، مستمساكته، ودفعاته. تُفتح بالضغط على اسم الزبون من
أي مكان في البرنامج (قائمة العملاء، التقارير، المتأخرات).

تعرض أعلى الصفحة: الاسم، الرقم الوطني، الهاتف، و**المتبقي** و**إجمالي القسط الشهري** لكل عملة على حدة
إن كان للزبون فواتير بعملتين مختلفتين.

**بطاقات الفواتير:** كل عملية بيع تظهر كبطاقة مستقلة تحمل رقم الفاتورة وتاريخها وإجمالي التقسيط وعدد
الأقساط، تليها:

- قائمة الأجهزة/المنتجات المباعة ضمن الفاتورة
- سطر **"الكفيل: [الاسم]"** — يظهر فقط إن كان لهذه الفاتورة تحديداً كفيل (الكفيل مرتبط بالفاتورة وليس
  بالزبون بشكل عام، فقد يكون لزبون واحد فواتير بكفلاء مختلفين)
- جدول الأقساط: تاريخ الاستحقاق، المبلغ، المتبقي، والحالة (`مسدد` / `مسدد جزئياً` / `غير مسدد`)

زر **عرض كشف الحساب** أعلى الصفحة يفتح كشفاً كاملاً قابلاً للطباعة (راجع قسم "كشف حساب الزبون").

---

## مستمسكات الزبون

معرض صور داخل صفحة الزبون لحفظ صور الهوية والعقود وأي وثيقة أخرى.

1. **إضافة صورة** — اضغط "+ إضافة صورة" أعلى البطاقة، واختر صورة واحدة أو عدة صور دفعة واحدة من جهازك.
2. **التصفح** — اضغط أي صورة مصغّرة لفتحها بالحجم الكامل، وتنقّل بزري "السابق/التالي" أو بأسهم لوحة
   المفاتيح ← →، وأغلق بمفتاح Esc أو زر "إغلاق".
3. **الحذف** — من داخل العارض بالحجم الكامل اضغط "حذف" — يُطلب تأكيد قبل الحذف النهائي.

> الصور تُحفظ داخل قاعدة البيانات نفسها (وليست ملفات منفصلة على القرص)، لذا فإن نسخة احتياطية واحدة
> لقاعدة البيانات تحفظ الزبائن والفواتير **ومستمساكتهم** معاً.

---

## تسجيل الدفعات

نموذج "تسجيل دفعة" في الشريط الجانبي لصفحة الزبون.

1. **الفاتورة** — اختر فاتورة محددة لتخصيص الدفعة لها فقط، أو اترك "كل الفواتير (تلقائي)" ليوزّع النظام
   المبلغ تلقائياً.
2. **تاريخ الدفعة** — تاريخ استلام المبلغ فعلياً.
3. **العملة** — دينار عراقي أو دولار أمريكي، يمكن أن تختلف عن عملة الفاتورة أو القسط.
4. **المبلغ** — القيمة المستلمة فعلياً بالعملة المختارة أعلاه.
5. **سعر الصرف اليدوي** — دينار عراقي مقابل دولار واحد؛ يُستخدم فقط إن اختلفت عملة الدفعة عن عملة القسط
   المطلوب سداده.
6. **تسجيل الدفعة** — يوزَّع المبلغ فوراً وتُحدَّث الجداول.

**كيف يوزَّع المبلغ؟**

- عند اختيار **فاتورة محددة**: يُوزَّع المبلغ على أقدم أقساطها غير المسددة فقط.
- عند اختيار **كل الفواتير**: يُوزَّع المبلغ على أقدم الأقساط غير المسددة من كل فواتير الزبون، بأي عملة
  كانت — يحوَّل المبلغ تلقائياً عند الحاجة حسب سعر الصرف المُدخل.

> إن تبقّى جزء من المبلغ بلا تخصيص (لا توجد أقساط مستحقة متبقية، أو الدفعة أكبر من إجمالي المستحق) يظهر
> تنبيه بالمبلغ غير المخصَّص فور الحفظ.

---

## المنتجات

قائمة الأجهزة القابلة للبيع بالتقسيط.

| الحقل | الوصف |
|---|---|
| اسم المنتج * | إلزامي |
| السعر النقدي المرجعي * | سعر البيع نقداً (وليس سعر التقسيط) — الأساس الذي يُحسب منه الهامش عند البيع |
| العملة * | دينار عراقي أو دولار أمريكي — تبقى ثابتة لهذا المنتج بعد إنشائه |

يظهر كل منتج في نموذج "بيع جديد" بغض النظر عن عملة الفاتورة المختارة — يُحوَّل السعر تلقائياً عند الحاجة.

---

## بيع جديد بالتقسيط

إنشاء فاتورة تقسيط كاملة من الصفر.

1. **الزبون** — اختر المشتري من القائمة.
2. **الكفيل (اختياري)** — أي زبون آخر مسجَّل في النظام، عدا المشتري نفسه.
3. **تاريخ البيع والعملة** — عملة الفاتورة التي ستُحسب بها كل الأقساط.
4. **المنتجات** — أضف صفاً لكل منتج مع الكمية (زر "+ إضافة منتج" لإضافة أكثر من صنف). منتج بعملة مختلفة
   عن عملة الفاتورة يُحوَّل تلقائياً حسب سعر الصرف أدناه.
5. **نوع الهامش وقيمته** — نسبة مئوية من السعر النقدي، أو مبلغ مقطوع يُضاف مباشرة.
6. **عدد الأقساط (أشهر)** — عدد الدفعات الشهرية المتفق عليها.
7. **سعر الصرف اليدوي** — دينار عراقي مقابل دولار واحد، يُحفظ مع الفاتورة بشكل دائم.
8. **مراجعة المعاينة ثم "إنشاء البيع"** — لوحة المعاينة تعرض السعر النقدي، الهامش، الإجمالي، والقسط
   التقريبي شهرياً قبل الحفظ.

> القيم النهائية تُحسب في قاعدة البيانات وقت الحفظ (وليس من المعاينة)، والباقي الكسري الناتج عن القسمة
> على عدد الأشهر يُضاف تلقائياً إلى **آخر** قسط، بحيث لا يضيع أي مبلغ.

---

## المتأخرات

نظرة شاملة على كل الأقساط المتأخرة في المتجر بأكمله، بلا حاجة لفتح كل زبون على حدة. جدول واحد يضم كل
الأقساط التي تجاوزت تاريخ استحقاقها ولم تُسدَّد بالكامل، مع اسم الزبون وعدد أيام التأخير والمبلغ
المتبقي. اضغط أي صف للانتقال مباشرة لصفحة ذلك الزبون.

شارة التأخير: `30 يوم فأقل` (تحذير) مقابل `أكثر من 30 يوم` (حرج).

---

## التقارير والتحليلات

تبويب "التقارير" — خمس بطاقات تحليلية، وكل واحدة قابلة للتصدير كملف CSV منفرد.

| البطاقة | ماذا تعرض |
|---|---|
| ملخص المبيعات والأرباح | عدد المبيعات، السعر النقدي، الربح (الهامش)، إجمالي التقسيط، المُحصَّل، والمتبقي — لكل عملة، مع تبديل الفترة: الكل / هذه السنة / هذا الشهر |
| الأكثر مبيعاً | أعلى 10 منتجات حسب الكمية المباعة والإيرادات |
| أكثر الزبائن شراءً | أعلى 10 زبائن حسب عدد العمليات وإجمالي المشتريات — اضغط أي اسم للانتقال لصفحته |
| الأكثر تأخراً بالتسديد | ترتيب الزبائن حسب عدد الأقساط المتأخرة وأطول مدة تأخير |
| نظرة عامة على الزبائن | جدول شامل لكل الزبائن: عدد المبيعات، المشتريات، المتبقي، وتاريخ آخر عملية |

> زر **تصدير CSV** أعلى كل بطاقة يُنزّل نسخة من الجدول المعروض فوراً، جاهزة للفتح في إكسل بدون مشاكل
> بالنصوص العربية.

---

## تقرير تحويل العملة

أسفل صفحة التقارير — يشرح بالتفصيل كل عملية حوَّل فيها النظام مبلغاً من عملة إلى أخرى. يظهر هذا التقرير
فقط الحالات التي جرى فيها تحويل فعلي (جهاز بعملة تختلف عن عملة فاتورته، أو دفعة بعملة تختلف عن عملة
القسط) — أي عملية لم تتضمن تحويلاً لا تُحسب هنا. لكل تحويل يُعرض سعر الصرف الفعلي المستخدم (دينار مقابل
دولار واحد).

| التبويب | المحتوى |
|---|---|
| حسب الفاتورة | جدولان: تحويلات أسعار الأجهزة ضمن كل فاتورة، وتحويلات الدفعات — بالسعر الأصلي والمحوَّل والكمية وسعر الصرف |
| حسب الزبون | إجمالي مجمَّع لكل زبون: عدد الأجهزة والدفعات المحوَّلة وقيمتها قبل وبعد التحويل |
| حسب الجهاز | نفس الفكرة، لكن مجمَّعة على مستوى كل منتج/جهاز بدلاً من الزبون |

---

## كشف حساب الزبون

صفحة طباعة مستقلة، تُفتح من زر "عرض كشف الحساب" أعلى صفحة أي زبون. تعرض بيانات الزبون، الرصيد المستحق
لكل عملة، كل فواتيره وجداول أقساطها كاملة، وسجل كل دفعاته.

> زر **طباعة / تصدير PDF** يفتح نافذة الطباعة القياسية في النظام — اختر طابعة "Save as PDF" أو
> "Microsoft Print to PDF" لحفظها كملف PDF بدل طباعتها ورقياً.

---

## النسخ الاحتياطي

بطاقة أعلى صفحة التقارير — نسخ قاعدة البيانات كاملة إلى ملف واحد للحفظ. زر "حفظ نسخة احتياطية" يفتح
قائمة بثلاثة خيارات:

| الخيار | التفاصيل |
|---|---|
| حفظ محلي (القرص الصلب) | نافذة حفظ عادية للتصفح إلى أي مجلد على الجهاز |
| حفظ في Google Drive أو OneDrive | نفس نافذة الحفظ، لكنها تبدأ تلقائياً من مجلد OneDrive إن كان مثبتاً على الجهاز؛ يمكن التصفح لمجلد Google Drive يدوياً بنفس الطريقة |
| التخزين السحابي (قريباً) | غير مفعَّل حالياً — مخصص لخدمة نسخ احتياطي سحابية مستقبلية |

الملف الناتج (`installment-manager-backup-YYYY-MM-DD.sqlite3`) نسخة كاملة ومتّسقة من قاعدة البيانات
لحظة الحفظ — تشمل الزبائن، الفواتير، الدفعات، وصور المستمسكات كلها في ملف واحد.

---

## تحديث البرنامج

تبويب "حول البرنامج" يعرض ثلاث بطاقات:

- **معلومات الشركة**: اسم الشركة، رقم الهاتف (رابط اتصال مباشر)، والبريد الإلكتروني (رابط مراسلة).
- **معلومات الترخيص**: (تظهر فقط إن كان الترخيص أو التجربة سارية) — لمن رُخِّص، تاريخ التفعيل، تاريخ
  النفاذ، ومدة الترخيص؛ مع شارة "نسخة تجريبية" إن كانت الفترة الحالية تجربة مجانية لا ترخيصاً مدفوعاً.
- **التحقق من التحديثات** (تفصيلها أدناه).

1. **التحقق من التحديثات** — يبحث البرنامج عن أحدث إصدار منشور على GitHub.
2. **إن وُجد تحديث** — يظهر رقم الإصدار الجديد وملاحظات الإصدار، مع زر "تحديث الآن".
3. **تحديث الآن** — يُنزَّل التحديث ويُثبَّت تلقائياً، ثم يُعاد تشغيل البرنامج بالإصدار الجديد.

> التحديث لا يمسّ قاعدة بياناتك (الزبائن، الفواتير، الترخيص المُفعَّل) — يستبدل ملفات البرنامج فقط،
> تماماً كما لو أعدت تشغيل ملف التنصيب يدوياً.

---

## الأمان وحماية البيانات

- **لا أخطاء تقريب:** كل المبالغ المالية تُخزَّن كأعداد صحيحة تماماً (سنتات للدولار، دينار كامل
  للعراقي) — لا أرقام عشرية قد تتراكم فيها أخطاء التقريب مع الوقت.
- **سجل غير قابل للتعديل:** عمليات البيع والدفع تُسجَّل ولا تُحذف أو تُعدَّل لاحقاً — أي تصحيح يكون
  بعملية جديدة، فيبقى السجل التاريخي كاملاً وقابلاً للتدقيق دائماً.
- **ترخيص مربوط بالجهاز:** موقَّع رقمياً ومربوط ببصمة عتاد الجهاز، مع حماية من إرجاع ساعة النظام
  للتحايل على تاريخ الانتهاء (راجع قسم "قبل البدء").
- **قاعدة بيانات واحدة:** ملف SQLite واحد يحوي كل البيانات بما فيها صور المستمسكات — نسخة احتياطية
  واحدة تكفي لحفظ كل شيء.
